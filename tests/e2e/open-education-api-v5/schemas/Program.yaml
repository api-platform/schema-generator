allOf:
  - $ref: '../schemas/ProgramId.yaml'
  - $ref: '../schemas/ProgramProperties.yaml'
  - properties: 
      validFrom:
        description: The first day this program is valid (inclusive).
        type: string
        format: date
      validTo:
        description: The day this program ceases to be valid (e.g. exclusive).
        type: string
        format: date
