language: php

php:
  - 5.4
  - 5.5

before_script:
  - composer install --dev --prefer-source
  - mkdir ../model

script:
  - php bin/schema.php ../model
  - php bin/schema.php ../model examples/config/lechoppe.yml